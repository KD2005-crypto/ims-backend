# 1. DATABASE CONNECTION
spring.datasource.url=jdbc:mysql://localhost:3306/codeb_ims
spring.datasource.username=root
spring.datasource.password=Saumay@05

# 2. HIBERNATE SETTINGS
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 3. GMAIL SETTINGS (SMTPS "Nuclear" Fix)
spring.mail.host=smtp.gmail.com
spring.mail.port=465

# ? Your new email address
spring.mail.username=dhanshrikalshetti2005@gmail.com

# ? The App Password (MUST belong to the email above!)
spring.mail.password=tajfgsrgtoknckqj

# ? PROTOCOL CHANGE: Switching to SMTPS to bypass timeout
spring.mail.protocol=smtps
spring.mail.properties.mail.transport.protocol=smtps
spring.mail.properties.mail.smtps.auth=true
spring.mail.properties.mail.smtps.ssl.enable=true
spring.mail.properties.mail.smtps.timeout=5000
spring.mail.properties.mail.smtps.starttls.enable=false